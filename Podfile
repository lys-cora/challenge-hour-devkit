# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'hour-message' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for hour-message

  target 'hour-messageTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'hour-messageUITests' do
    # Pods for testing
  end

end
